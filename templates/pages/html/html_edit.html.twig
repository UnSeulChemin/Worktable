{% extends 'base.html.twig' %}

{% block title %}HTML - Update{% endblock %}

{% block body %}

<h2 class="padding-bottom-none">Formulaire HTML</h2>

<div class="hr"></div>

<h2>Modifier HTML</h2>

{{ form_start(editFormHtml) }}

    {# Flash Messages #}
    {% for message in app.flashes('success') %}
        <div class="success-flash">
            <p>{{ message }}</p>
        </div>
    {% endfor %}

    {# Flash Messages #}
    {% for message in app.flashes('warning') %}
        <div class="warning-flash">
            <p>{{ message }}</p>
            {{ form_errors(editFormHtml.name) }}
        </div>
    {% endfor %}

    <div class="card">

        <div>
            {{ form_label(editFormHtml.name) }}
            {{ form_widget(editFormHtml.name) }}
        </div>

        <div>
            {{ form_label(editFormHtml.submit) }}
            {{ form_widget(editFormHtml.submit) }}
        </div>

    </div>

{{ form_end(editFormHtml) }}

{% endblock %}