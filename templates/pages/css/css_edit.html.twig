{% extends 'base.html.twig' %}

{% block title %}CSS - Update{% endblock %}

{% block body %}

<h2 class="padding-bottom-none">Formulaire CSS</h2>

<div class="hr"></div>

<h2>Modifier CSS</h2>

{{ form_start(editFormCss) }}

    {# Flash Messages #}
    {% for message in app.flashes('success') %}
        <div class="success-flash">
            <p>{{ message }}</p>
        </div>
    {% endfor %}

    {# Flash Messages #}
    {% for message in app.flashes('warning') %}
        <div class="warning-flash">
            <p>{{ message }}</p>
            {{ form_errors(editFormCss.name) }}
        </div>
    {% endfor %}

    <div class="card">

        <div>
            {{ form_label(editFormCss.name) }}
            {{ form_widget(editFormCss.name) }}
        </div>

        <div>
            {{ form_label(editFormCss.submit) }}
            {{ form_widget(editFormCss.submit) }}
        </div>

    </div>

{{ form_end(editFormCss) }}

{% endblock %}